# This env file stores environmental variables used by all test cases! Any case specific env vars should be in their corresponding directory.
DOCKERIMAGE="cybergisx/pysal-access:0.0.4"
WORKINGDIR=$(pwd)  # this directory
# all of these paths should be relative to the WORKINGDIR or absolute paths
result_folder="result/"  # the directory where results should be placed
herop_data_folder="/Users/ahyaan/herop_access_data"  # the directory where default data is stored